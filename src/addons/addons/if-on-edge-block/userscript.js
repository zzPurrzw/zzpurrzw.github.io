export default async function ({ addon, msg, console }) {
  // This is an internal implementation addon that converts the custom
  // "if on edge" procedure into a built-in block in the motion category
  
  // The block is already defined in the Scratch VM, we just need to
  // add it to the toolbox in motion category
}
